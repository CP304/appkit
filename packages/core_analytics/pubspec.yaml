name: core_analytics
description: Firebase Analytics/Crashlytics hooks (optional)
version: 0.1.0
publish_to: "none"
environment:
  sdk: ">=3.4.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  # firebase_core: ^3.5.0   # Aktivieren, sobald Firebase konfiguriert ist
  # firebase_analytics: ^11.3.0
  # firebase_crashlytics: ^4.0.4
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
