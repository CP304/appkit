# Store Prep
- Screenshots über `integration_test` + `screenshots.yaml` (Platzhalter).
- Changelog Split‑Script in `tools/scripts/changelog_split.sh`.
- Icons/Splash via `flutter_launcher_icons`, `flutter_native_splash` (siehe pubspec).
